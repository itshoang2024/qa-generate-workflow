1. /projects                   — List + create projects, pick existing for DELTA
2. /projects/[id]              — Project detail: GDD version history, runs for this project, trigger new run
3. /runs/[id]                  — Run dashboard: timeline + coverage summary + tabs to artifacts
4. /runs/[id]/hil/[tier]       — HIL queue (0/1/2/3) grouped by reviewer/feature/epic
5. /runs/[id]/sync-log         — Sync events filtered by phase
6. /runs/[id]/risk             — Risk events + kill switch status
7. /runs/[id]/sign-off         — Final coverage report + sign-off button